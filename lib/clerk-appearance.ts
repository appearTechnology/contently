/**
 * Shared Clerk UI tweaks. Passed to `ClerkProvider` so all embedded components inherit it.
 */
export const clerkAppearance = {
  elements: {
    card: "shadow-none",
    cardBox: "shadow-none",
  },
} as const;
